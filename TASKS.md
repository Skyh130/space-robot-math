# TASKS.md — 작업 순서

한 번에 한 Phase씩 진행한다. **완료 조건을 만족하기 전에 다음 Phase로 넘어가지 않는다.**
MVP는 Phase 6까지다.

---

## Phase 0 — 프로젝트 셋업

- Vite + React + TypeScript(strict) + Tailwind 초기화
- Vitest 설정
- 세로 모드 고정, safe-area, 최대 폭 480px 중앙 정렬 레이아웃 셸
- `CLAUDE.md`의 컬러 토큰을 Tailwind 테마에 등록
- 폰트 로드 (Jua / Pretendard)

**완료 조건**: 빈 화면이 폰에서 세로로 뜨고, 배경색과 폰트가 적용된다.

---

## Phase 1 — 문제 엔진

- `engine/types.ts`: `QuestionTemplate`, `Question`, `AnswerResult`, `SkillKey` 정의
- `engine/rng.ts`: 시드 기반 난수
- `engine/generator.ts`: 템플릿 + 시드 → 문제 인스턴스
- 4지선다 오답 보기 생성기 (실수 유형 기반, 무작위 금지)
- **Vitest 테스트**: 템플릿별 20회 이상 생성 후 정답 검증

**완료 조건**: `npm test` 통과. UI는 아직 없다.
**주의**: 이 Phase를 대충 넘기면 이후 전부 다시 만든다. 테스트가 핵심 산출물이다.

---

## Phase 2 — 한 문제 풀기

- `QuestionCard`, `NumPad`, `ChoiceGrid`, `Feedback` 컴포넌트
- 하드코딩한 문제 1개를 실제로 풀고 정답/오답 피드백까지 동작
- 오답 시 정답 + 한 줄 이유 + 재시도

**완료 조건**: 폰 화면에서 손가락으로 문제 하나를 끝까지 풀 수 있다. 스크롤이 생기지 않는다.

---

## Phase 3 — 월드 1 전체

- `data/world1.ts`: Lv1~Lv5 템플릿 작성 (설계서 5장 참고)
- 스테이지 = 문제 8개 연속 진행
- 진행 인디케이터 (남은 문제 수, 시간 표시 아님)
- `ResultScreen`: 정답 수 → 별 1~3개
- 보스 스테이지 1개

**완료 조건**: 월드 1의 5개 스테이지 + 보스를 처음부터 끝까지 클리어할 수 있다.

---

## Phase 4 — 저장과 월드맵

- `state/save.ts`: localStorage 저장/로드, 스키마 버전 필드 포함
- 저장 항목: 별, 클리어 여부, 획득 부품, 코인, `skillStats`
- `WorldMapScreen`: 행성 8개 표시, 미해금 월드는 잠금
- 앱을 껐다 켜도 진행이 유지된다

**완료 조건**: 브라우저를 완전히 종료 후 재실행해도 별과 진행 상황이 그대로다.

---

## Phase 5 — 월드 2, 3

- `data/world2.ts`: 덧셈·뺄셈 Lv1~Lv5 + 보스
- `data/world3.ts`: 곱셈구구 Lv1~Lv5 + 보스
- W2 세로셈 힌트 애니메이션 (받아올림 1이 올라가는 시각화)
- W3 보스전 60초 타이머 — **여기가 유일한 타이머 사용처**
- 같은 세션 내 오답 재출제 (3문제 뒤, 숫자만 변경)

**완료 조건**: 월드 1~3 전체 플레이 가능. 각 월드 생성기 테스트 통과.

---

## Phase 6 — 격납고와 PWA (MVP 완성)

- `HangarScreen`: 획득한 부품이 로봇에 붙어 있는 모습 (SVG)
- 부품 획득 연출 — **가장 공들일 화면**. 다른 화면의 연출은 여기를 위해 절제한다
- PWA 매니페스트 + 아이콘, 홈 화면 추가 가능
- 오프라인 동작 확인

**완료 조건**: 아이 태블릿 홈 화면에 아이콘을 추가하고, 비행기 모드에서 플레이된다.

---

# 여기까지가 MVP — 반드시 아이에게 먼저 줘볼 것

관찰 항목:
- 설명 없이 혼자 첫 스테이지를 끝내는가
- 한 번에 몇 분 하는가 (10분 미만이면 보상 밀도 부족)
- **다음 날 스스로 켜는가** ← 가장 중요
- 틀렸을 때 표정 (좌절하면 힌트·난이도 재조정)
- 부품 획득 순간에 반응이 있는가 (없으면 연출 강화)

이 결과에 따라 Phase 7 이후 우선순위를 바꾼다.

---

## Phase 7 — 적응형 난이도

- `engine/adaptive.ts`: 최근 5문제 정답률 80%↑ → +1, 40%↓ → −1
- `engine/reviewQueue.ts`: 세션 간 복습 큐, 취약 skill 우선 편성
- **UI에 어떤 흔적도 남기지 않는다**

## Phase 8 — 월드 4~8

- W4 시계 (바늘 드래그), W5 도형, W6 나눗셈, W7 분수·소수, W8 종합
- W7은 시각 자료 비중을 다른 월드의 2배로
- 최종 보스 3라운드 + 합체 엔딩 연출

## Phase 9 — 코인 상점

- 로봇 컬러·데칼 구입 (실력과 무관한 순수 재미 요소)
- 연속 출석 보상

## Phase 10 — 부모 모드

- 숨겨진 진입 (설정 아이콘 길게 누르기 등)
- 영역별 정답률 그래프, 오답 노트, 플레이 시간 확인
- 하루 플레이 시간 제한 설정
- 문제 읽어주기(TTS)

---

## Claude Code 첫 프롬프트

```
docs/설계서.md와 CLAUDE.md, TASKS.md를 먼저 읽어줘.
Phase 0부터 시작한다. Phase 0 완료 조건을 만족하면 멈추고 결과를 보고해줘.
설계서에 없는 기능은 임의로 추가하지 말고 먼저 물어봐.
```
