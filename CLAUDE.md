# CLAUDE.md — 우주 로봇 수학 모험

이 파일은 이 저장소에서 작업할 때 반드시 따라야 할 규칙이다.
상세 기획은 `docs/설계서.md` 참고.

---

## 프로젝트 한 줄 요약

초등학교 2학년 아이가 태블릿·스마트폰으로 하는 수학 학습 게임.
행성을 하나씩 클리어하며 로봇 부품을 모아 최종 합체한다.

**실제 사용자는 8살 아이 한 명이다.** 모든 판단의 기준은 "8살이 어른 도움 없이 할 수 있는가"이다.

---

## 기술 스택

| 항목 | 선택 | 비고 |
|---|---|---|
| 빌드 | Vite | |
| 프레임워크 | React 18 + TypeScript | strict mode |
| 스타일 | Tailwind CSS | |
| 상태 | React 상태 + Context | 상태 라이브러리 도입 금지 |
| 저장 | localStorage | 서버 없음 |
| 테스트 | Vitest | 문제 생성기 전용 |
| 배포 | 정적 호스팅 + PWA | 홈 화면 추가로 앱처럼 실행 |

**백엔드는 만들지 않는다.** 로그인, 계정, 서버 저장 모두 범위 밖.

---

## 디렉터리 구조

```
src/
  engine/
    types.ts          문제·정답·진행 상태 타입
    generator.ts      템플릿 → 문제 인스턴스 생성
    adaptive.ts       숨은 난이도 조절
    reviewQueue.ts    오답 재출제 큐
    rng.ts            시드 기반 난수 (테스트 재현용)
  data/
    world1.ts ~ world8.ts   문제 템플릿 정의
    worlds.ts               월드 메타데이터
  state/
    save.ts           localStorage 읽기/쓰기, 스키마 버전 관리
    useProgress.ts    진행 상태 훅
  screens/
    TitleScreen.tsx
    WorldMapScreen.tsx
    StageScreen.tsx
    ResultScreen.tsx
    HangarScreen.tsx  로봇 조립 현황
  components/
    NumPad.tsx        화면 숫자패드
    ChoiceGrid.tsx    4지선다
    QuestionCard.tsx
    Feedback.tsx      정답/오답 피드백
    StarRating.tsx
  assets/             SVG만. 외부 이미지 파일 금지
```

---

## 절대 규칙

### 1. 정답이 틀리면 안 된다
수학 게임에서 오답 키는 치명적이다. **모든 문제 생성기는 Vitest 단위 테스트를 함께 작성한다.**
각 템플릿마다 파라미터 경계값 포함 최소 20회 생성 → 정답 검증. 테스트 없는 생성기는 머지하지 않는다.

### 2. 난이도 변화를 화면에 표시하지 않는다
적응형 조절은 내부 로직으로만 동작한다. "쉬운 문제로 바꿨어요" 같은 문구, 레벨 하락 표시, 난이도 뱃지 전부 금지.

### 3. 타이머는 W3 보스전에만 쓴다
그 외 모든 화면에 카운트다운, 남은 시간 바, 시간 압박 연출 금지.

### 4. 실패 패널티를 만들지 않는다
체력 0 → 처음부터 다시 하는 구조 금지. 틀리면 힌트 보여주고 재시도.
오답 연출은 짧고 담백하게. 빨간 X 큰 화면, 부정적 효과음, 슬픈 캐릭터 표정 금지.

### 5. 저작권
기존 로봇 IP(건담 등)의 이름·디자인·형태를 사용하지 않는다. 모든 로봇은 오리지널 SVG로 그린다.

### 6. 세로 모드 고정
가로 레이아웃을 만들지 않는다. `orientation: portrait`, safe-area inset 처리 필수.

---

## UI 규격

- 세로 모드, 뷰포트 기준 폭 360~430px 우선 (태블릿은 최대 폭 480px로 중앙 정렬)
- 터치 타깃 최소 48×48px, 숫자패드 키는 64×64px
- 문제 텍스트 20px 이상, 숫자 32px 이상
- 문제는 화면 **상단**, 입력은 **하단**. 손이 문제를 가리면 안 된다
- 정답 피드백은 0.5초 이내에 시작
- 오답 시: 정답 + **한 줄 이유** + 그림 힌트. 정답만 던지고 넘어가지 않는다
- `prefers-reduced-motion` 존중
- 스크롤 없이 한 화면에 문제 하나가 다 들어와야 한다

---

## 비주얼 방향

장난감 스티커 느낌. 두꺼운 외곽선 + 채도 높은 색 + 뭉툭한 라운드.
얇은 선, 파스텔, 반투명 유리 효과, 그라데이션 워시는 쓰지 않는다.

```
--deep     #1B2A6B   우주 배경
--panel    #2C3E8F   패널
--energy   #FFC93C   에너지·정답·별
--coral    #FF6B5B   보스·강조
--mint     #4FD1C5   보조·힌트
--paper    #FFF6E5   텍스트·카드 바탕
--outline  #101838   모든 요소의 외곽선 (2~3px)
```

- 모든 버튼·카드·캐릭터에 `--outline` 2~3px 외곽선을 넣는다. 이게 이 게임의 시그니처다
- 그림자는 흐린 회색 그림자 대신 **아래로 4px 오프셋된 단색 그림자**(hard shadow)
- 제목: `Jua` 또는 `BM Dohyeon` / 본문·숫자: `Pretendard`
- 숫자는 `font-variant-numeric: tabular-nums` 로 자릿수 흔들림 방지
- 화려한 연출은 **부품 획득 순간 한 곳**에만 몰아준다. 나머지 화면은 조용하게

---

## 문제 템플릿 작성 규칙

```ts
{
  id: "w3_lv4_blank",
  world: 3,
  level: 4,
  skill: "multiplication_table",   // 통계 집계 키
  inputType: "numpad",             // numpad | choice | drag | order
  render: (p) => `${p.a} × □ = ${p.a * p.b}`,
  params: { a: [2, 9], b: [2, 9] },
  answer: (p) => p.b,
  hint: (p) => `${p.a}단을 순서대로 세어 볼까?`,
}
```

- 생성기는 **순수 함수**로 작성한다. 시드를 받아 같은 입력이면 같은 문제가 나와야 한다
- 4지선다 오답 보기는 무작위로 만들지 말고 **아이가 실제로 하는 실수**를 넣는다
  - 받아올림 누락 (37+45 → 72)
  - 자릿수 밀림
  - 곱셈을 덧셈으로 (4×6 → 10)
- 같은 세션에서 동일 파라미터 조합이 반복되지 않게 최근 출제 이력을 확인한다

---

## 텍스트 작성 규칙

- 2학년이 읽는 글이다. 한 문장 15자 내외, 어려운 한자어 금지
- 문제 지시문은 짧게: "빈칸에 알맞은 수는?" (O) / "다음 식의 □ 안에 들어갈 알맞은 수를 구하시오." (X)
- 버튼은 동작을 그대로 쓴다: "출발!" "다시 하기" "격납고로"
- 오답 문구에 사과나 부정 표현을 쓰지 않는다. "아까워! 다시 볼까?" 정도

---

## 커밋·작업 방식

- 한 번에 한 Phase씩. Phase 완료 조건은 `TASKS.md` 참고
- 새 기능 추가 전에 기존 테스트를 통과시킨다
- 리팩터링과 기능 추가를 한 커밋에 섞지 않는다
- 임의로 범위를 넓히지 않는다. 설계서에 없는 기능은 먼저 물어본다
